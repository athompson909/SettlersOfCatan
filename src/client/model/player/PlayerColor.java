package client.model.player;

/**
 * These are the colors the player can choose from when starting a game.
 * Each player must be a different color.
 */
public enum PlayerColor {
    RED,
    ORANGE,
    YELLOW,
    BLUE,
    GREEN,
    PURPLE,
    PUCE,
    WHITE,
    BROWN;
}
